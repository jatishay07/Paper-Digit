# Paper-Digit
A computer vision project that detects single handwritten digits from a webcam feed and predicts their values in real-time using a **custom-built neural network** trained on the **MNIST dataset**.
